#!/bin/bash

# Required: Raycast Script Command Metadata
# @raycast.schemaVersion 1
# @raycast.title Winners & Losers
# @raycast.mode fullOutput
# @raycast.packageName Investment Portfolio
# @raycast.icon 📈
# @raycast.description Top movers between last two snapshots

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_ROOT"
exec uv run python3 "$SCRIPT_DIR/lib/portfolio-winners-losers_impl.py" "$@"
