#!/bin/bash

# Required: Raycast Script Command Metadata
# @raycast.schemaVersion 1
# @raycast.title Portfolio Status
# @raycast.mode fullOutput
# @raycast.packageName Investment Portfolio
# @raycast.icon 💼
# @raycast.description View current portfolio positions (live from spreadsheet)

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_ROOT"
exec uv run python3 "$SCRIPT_DIR/lib/portfolio-status_impl.py" "$@"
